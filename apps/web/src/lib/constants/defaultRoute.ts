export const DEFAULT_ROUTE = '/component/getting-started';

/**
 * Where leaving onboarding lands when no deep link was preserved (`?next=`)
 */
export const AFTER_SETUP_ROUTE = '/component/getting-started';
