[package]
description = "xtask kafka-topics: generate/verify .github/kafka-cluster-topics.json from conation_event_topics"
edition = "2024"
name = "xtask_kafka_topics"
publish = false
version = "0.1.0"

[[bin]]
name = "xtask_kafka_topics"
path = "src/main.rs"

[dependencies]
anyhow = { workspace = true }
serde_json = { workspace = true }
# Source of truth for the declared topics.
conation_event_topics = { path = "../../../../crates/conation_event_topics" }
xtask_paths = { path = "../xtask_paths" }
