.direnv/*
.envrc
**/.env*
**/*.env
**/*.env.*
**/.dev.vars*
# Tracked template for Mac desktop → this Linux local stack.
!.env.desktop.local.example
.idea/
**/.DS_Store
.run/
.claude/.local
temp/
run-local.fish
**/node_modules
**/.wrangler
**/dist
**/test-results
**/playwright-report
**/.playwright-mcp/
**/coverage
!packages/sdk/src/coverage/
!packages/sdk/src/coverage/**
**/.nyc_output
**/.cache
**/.vite
**/build
# `**/build` is for JS build output. This is a Rust source module that happens
# to be named `build`, and the rule was silently dropping new files added to it
# (a new `test.rs` there is ignored, and `git status` stays clean about it).
!/tooling/xtask/crates/xtask_local/src/local/build/
**/*.tsbuildinfo
**/*.log
**/result
**/result-*
.pi/plan/
.pi/plans/
# Per-instance generated local-stack artifacts (compose override, env, kickstart, Caddyfile).
infra/local/generated/
**/target/
/target
/previous.md
**/ai_tools/schemas/
.pi/outputs/
.commandcode/
**/.webhook.json
/artifacts

# User-supplied brand originals are preserved at the workspace root. Canonical
# byte-identical masters live under apps/web/public/brand and are tracked.
/con copy.png
/cona copy.png
/conation C copy.png

# Raw SSE captures from CURSOR_ACP_RECORD_DIR; sanitize into
# crates/cursor_acp/fixtures/real/ before committing one.
/recordings/

# Per-developer macrod config contains this machine's harness bearer token.
/macrod.toml
/macrod.webhook-state.json

# Owner-bound subscription OAuth credentials for the internal Claude demo.
/.claude-cloud/
