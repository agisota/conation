[package]
edition = "2024"
name = "task_dedup"
publish = false
version = "0.1.0"

[[bin]]
name = "backfill_task_embeddings"
path = "src/bin/backfill_task_embeddings.rs"
required-features = ["backfill"]

[[bin]]
name = "pull_task_corpus"
path = "src/bin/pull_task_corpus.rs"
required-features = ["backfill"]

[[bin]]
name = "expand_eval_corpus"
path = "src/bin/expand_eval_corpus.rs"
required-features = ["backfill"]

[features]
# Pulls in everything the `backfill_task_embeddings` binary needs. The library
# itself does not require any of these, so default builds stay lean.
backfill = [
  "dep:aws-sdk-secretsmanager",
  "dep:clap",
  "dep:conation_aws_config",
  "dep:conation_entrypoint",
  "dep:conation_env",
  "dep:secretsmanager_client",
  "dep:tokio",
  "dep:conation_env_var"
]

default = ["backfill"]

[dependencies]
rootcause = { workspace = true }
agent = { path = "../agent" }
ai_usage = { path = "../ai_usage" }
anyhow = { workspace = true }
async-trait = { workspace = true }
connection_gateway_client = { path = "../connection_gateway_client" }
embedding = { path = "../embedding" }
futures = { workspace = true }
lexical_client = { path = "../lexical_client" }
model-entity = { path = "../model-entity" }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sqlx = { workspace = true }
system_properties = { path = "../system_properties" }
thiserror = { workspace = true }
tracing = { workspace = true }
utoipa = { workspace = true }
uuid = { workspace = true }

# Optional — only compiled in for the `backfill` feature (the backfill binary).
aws-sdk-secretsmanager = { workspace = true, optional = true }
clap = { workspace = true, optional = true }
conation_aws_config = { path = "../conation_aws_config", optional = true }
conation_entrypoint = { package = "macro_entrypoint", path = "../conation_entrypoint", optional = true }
conation_env = { path = "../conation_env", optional = true }
secretsmanager_client = { path = "../secretsmanager_client", optional = true }
tokio = { workspace = true, features = [
  "macros",
  "rt-multi-thread",
], optional = true }
conation_env_var = { path = "../conation_env_var", optional = true }

conation_service_urls = { path = "../conation_service_urls" }
workspace-hack = { version = "0.1", path = "../workspace-hack" }

[dev-dependencies]
automod = { workspace = true }
conation_db_migrator = { path = "../conation_db_migrator" }
conation_env_var = { path = "../conation_env_var" }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
