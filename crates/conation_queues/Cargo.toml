[package]
edition = "2024"
name = "conation_queues"
publish = false
version = "0.1.0"

[dependencies]
conation_env = { path = "../conation_env" }
paste = { workspace = true }
serde = { workspace = true }
workspace-hack = { version = "0.1", path = "../workspace-hack" }
