[package]
edition = "2024"
name = "conation_cors"
publish = false
version = "0.1.0"

[dependencies]
axum = { workspace = true }
tower-http = { workspace = true }
workspace-hack = { version = "0.1", path = "../workspace-hack" }
